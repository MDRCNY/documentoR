% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documentoR.R
\name{document_project_single_file}
\alias{document_project_single_file}
\title{Create a single combined documentation file only (no index.json)}
\usage{
document_project_single_file(
  path = ".",
  chat_fn,
  output_file = "documentation/documentation.md",
  output_base = getwd(),
  include = c("R", "r", "Rmd", "rmd", "qmd", "qmarkdown", "md", "markdown", "html",
    "htm", "ipynb", "pdf", "docx", "doc", "pptx", "rds", "rda", "rdata", "csv", "sql",
    "json", "yml", "yaml"),
  exclude_dirs = c("packrat", "renv", "node_modules", ".git", "demo"),
  exclude_patterns = c("\\\\.Rproj$", "^README\\\\.md$"),
  max_chars = 16000,
  verbose = TRUE,
  exclude_file = NULL,
  dry_run = FALSE,
  generate_readme = FALSE,
  overwrite_readme = FALSE,
  change_only = FALSE
)
}
\arguments{
\item{path}{Project root (default ".")}

\item{chat_fn}{Function(prompt)->character used to summarize each file. Required unless dry_run = TRUE.}

\item{output_file}{Path to the single combined markdown to write (default "documentation/documentation.md")}

\item{output_base}{"path" | "project" | path string. If omitted, defaults to getwd().}

\item{include}{File extensions to include}

\item{exclude_dirs}{Directories to exclude (simple names)}

\item{exclude_patterns}{Regex patterns to exclude (file names/paths)}

\item{max_chars}{Max characters of source to include in prompt}

\item{verbose}{Logical}

\item{exclude_file}{Optional path to an exclude.txt (see read_exclude_file)}

\item{dry_run}{Logical. If TRUE, only list files that would be summarized (no LLM calls, no writes).}

\item{generate_readme}{Logical. If TRUE, also create README.md from summaries.}

\item{overwrite_readme}{Logical. If TRUE, overwrite existing README.md when generate_readme = TRUE.}

\item{change_only}{Logical. If TRUE and output_dir/index.json exists, reuse unchanged summaries.}
}
\value{
Invisibly the path(s) to the written documentation file (and README if requested).
}
\description{
Summarizes project files via \code{chat_fn} and writes exactly one markdown file.
}
